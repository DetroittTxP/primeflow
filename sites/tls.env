# docker compose --env-file sites/tls.env -f docker-compose.sites.yml up -d
# Turns on the TLS edge: workers go through Caddy on 443 instead of straight to the server.
COMPOSE_PROFILES=tls
SITE_API_URL=https://edge
